import 'package:flutter/material.dart';

class Gambit extends StatelessWidget {
const Gambit({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return const Scaffold(
      body: Center(
        child: Text("Testing Doang"),
      ),
    );
  }
}